export class ItemMenuModel {
  title: string;
  titleClick: string;
  rowItems: RowMenuModel[];
}
export class RowMenuModel {
  title: string;
  hrefStr: string;
  iconClass: string;
  targetStr: string;
}
